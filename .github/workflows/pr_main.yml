name: PR against main

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  lint:
    uses: ./.github/workflows/run-command.yml
    with:
      command_name: lint
      command: just lint

  test:
    uses: ./.github/workflows/run-command.yml
    with:
      command_name: test
      command: just test

  examples:
    uses: ./.github/workflows/run-command.yml
    with:
      command_name: examples
      command: just examples

  docs:
    uses: ./.github/workflows/run-command.yml
    with:
      command_name: docs
      command: just docs
