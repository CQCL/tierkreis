Graph main() -> (
    int: Bool,
    flt: Bool,
    less: Bool,
    more: Bool,
    and1: Bool,
    and2: Bool,
    or1: Bool,
    or2: Bool,
    xor1: Bool,
    xor2: Bool
) {
    output (
        int: eq(ipow(imod(isub(idiv(imul(iadd(2, 3), 10), float_to_int(5.0)), 3), 5), 2), 4),
        flt: eq(fpow(fmod(fsub(fdiv(fmul(fadd(2.0, int_to_float(3)), 10.0), 5.0), 3.0), 3.0), 0.5), 1.0),
        less: and(and(ilt(2, 3), flt(2.1, 2.3)), and(ileq(2, 2), fleq(2.2, 2.2))),
        more: and(and(igt(3, 2), fgt(1.1, 0.2)), and(igeq(2, 2), fgeq(2.2, 2.2))),
        and1: and(true, true),
        and2: neq(and(true, false), true),
        or1: or(true, false),
        or2: eq(or(false, false), false),
        xor1: xor(true, false),
        xor2: eq(xor(true, true), false)
        );
}
