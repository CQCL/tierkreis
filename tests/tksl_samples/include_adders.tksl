Graph add2(x: Int) -> (y: Int) {
    output (y: iadd(x, 2));
}

Graph add5(x: Int) -> (y: Int) {
    output (y: iadd(x, 5));
}

Graph dbl(x: Int) -> (x: Int) {
    // positional multiple outputs
    output (iadd(copy(x)));
}
